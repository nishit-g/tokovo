# @tokovo/apps-instagram

This package contains the Instagram app simulation logic and UI components.

## Structure

- `src/runtime.ts`: Contains the Redux-like reducer for Instagram state (messages, typing, etc.)
- `src/ui.tsx`: React components that replicate the Instagram UI
- `src/types.ts`: TypeScript definitions
