/**
 * {{ title }}
 *
 * Generated by: pnpm turbo gen episode
 */

import { defineEpisode } from "@tokovo/episodes";
import { episode } from "@tokovo/dsl";

export default defineEpisode({
  meta: {
    id: "{{ id }}",
    title: "{{ title }}",
    category: "{{ category }}",
    {{#if description}}
    description: "{{ description }}",
    {{/if}}
    tags: [],
  },
  config: {
    format: "{{ format }}",
    durationInFrames: {{ durationInFrames }},
    apps: ["{{ appId }}"],
  },
  build: () =>
    episode("{{ id }}", {
      fps: {{ fps }},
      duration: "{{ durationSeconds }}s",
      title: "{{ title }}",
    })
      .device("phone", "iphone16", { app: "{{ appId }}" })
      // TODO: Add tracks
      .build(),
});

