import type { TokovoPluginContract, PluginViews } from "@tokovo/core";
import type { PluginManagerClass } from "@tokovo/react";
import { {{ pascalCase name }}View } from "./ui";
import { {{ camelCase name }}Reducer } from "./runtime/reducer";
import { {{ camelCase name }}Lowering } from "./lowering";
import { {{ camelCase name }}LayoutStrategies } from "./layout";
import { {{ camelCase name }}Anchors } from "./runtime/adapters/anchors";
import { create{{ pascalCase name }}InitialState } from "./runtime/state";

// Assemble views object
const {{ camelCase name }}Views: PluginViews = {
  AppRoot: {{ pascalCase name }}View,
};

// Plugin contract definition
export const {{ pascalCase name }}Plugin: TokovoPluginContract<"app_{{ name }}"> & {
  v2Lowering: typeof {{ camelCase name }}Lowering;
} = {
  id: "app_{{ name }}",
  version: "0.0.0",
  displayName: "{{ displayName }}",
  views: {{ camelCase name }}Views,
  reducer: {{ camelCase name }}Reducer,
  createInitialState: create{{ pascalCase name }}InitialState,
  eventKinds: [
    "ADD_NOTE",
    "UPDATE_NOTE",
    "DELETE_NOTE",
    "SET_ACTIVE_NOTE",
    "SET_SCREEN",
  ] as const,
  v2Lowering: {{ camelCase name }}Lowering,
  layouts: {{ camelCase name }}LayoutStrategies,
  anchors: {{ camelCase name }}Anchors,
};

// Registration function with guard
const registeredManagers = new WeakSet<PluginManagerClass>();

export function register{{ pascalCase name }}Plugin(
  pluginManager: PluginManagerClass,
): void {
  if (registeredManagers.has(pluginManager)) return;
  registeredManagers.add(pluginManager);
  pluginManager.register({{ pascalCase name }}Plugin);
}
