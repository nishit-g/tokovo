# Assets and Config

## Assets
- Register assets (icons/sounds/images) via plugin `assets`.
- Avoid inline paths in UI code.

## Config Defaults
- Use TokovoConfig for FPS and rendering defaults.
- Prefer constants from core types for camera/audio.
