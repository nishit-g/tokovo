# Anchors and DSL Reference

Use anchors when a scene should target semantic UI regions instead of raw coordinates.

Use the DSL when a scene needs explicit timing and choreography.

Use compile-time plugins when repetitive behavior should be generated consistently rather than written by hand.

## Practical Rule

- anchors: stable targeting
- DSL: explicit beats
- plugins: repetitive authoring acceleration
