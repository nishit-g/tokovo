# Rendering at Scale

Tokovo scales best when the control plane and render plane stay separated.

## Recommended Approach

- keep creator setup in Payload
- export manifests into the repo
- build and render from committed artifacts
- verify a real episode render path regularly

This avoids drift between admin state and render state.
