# Cinematic Subjects and DSL Reference

Use cinematic subjects when a scene should target stable UI meaning instead of raw coordinates.

Use the DSL when a scene needs explicit timing and choreography.

Use compile-time plugins when repetitive behavior should be generated consistently rather than written by hand.

## Practical Rule

- subjects: stable, typed targeting from canonical layout geometry
- DSL: explicit beats
- plugins: repetitive authoring acceleration
