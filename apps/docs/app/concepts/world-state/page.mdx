# World State

`WorldState` is the deterministic runtime snapshot at time `t`.

Payload entities and episode manifests are upstream inputs. By the time Tokovo computes `WorldState`, those inputs have already been resolved into concrete device, actor, and app configuration.
