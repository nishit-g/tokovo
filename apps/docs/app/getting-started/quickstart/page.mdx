# Quickstart

1. Create or edit an episode in `packages/episodes/src`.
2. Register it in the appropriate catalog.
3. Preview it with `video-runner`.
4. Render it locally.

Reference examples:

- `packages/episodes/src/production/entity-command-center.episode.ts`
